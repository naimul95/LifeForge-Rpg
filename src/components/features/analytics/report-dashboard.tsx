"use client";

import type { ReactNode } from "react";
import { Bar, BarChart, CartesianGrid, Line, LineChart, ResponsiveContainer, Tooltip, XAxis, YAxis } from "recharts";
import { Award, BookOpen, Flame, Map, Medal, Route, Sparkles, Trophy, Zap } from "lucide-react";

import type { AnalyticsDashboardData } from "@/types/analytics-dashboard";

const tooltipStyle = { contentStyle: { background: "#101827", border: "1px solid rgba(255,255,255,.12)", borderRadius: 8 }, itemStyle: { color: "#67e8f9" } };
function minutes(value: number) { return value < 60 ? `${Math.round(value)}m` : `${Math.floor(value / 60)}h ${Math.round(value % 60)}m`; }

export function AnalyticsReportDashboard({ data }: { data: AnalyticsDashboardData }) {
  const maxHeat = Math.max(...data.heatmap.map((item) => item.minutes), 1);
  return <main className="mx-auto max-w-375 px-5 pb-12 pt-7 sm:px-8 lg:px-10"><div className="mb-7"><p className="eyebrow text-cyan-300">Personal signal intelligence</p><h1 className="mt-2 text-3xl font-semibold tracking-tight text-white sm:text-4xl">Analytics</h1><p className="mt-2 text-sm text-slate-500">A responsive read on learning, consistency, and momentum.</p></div><div className="grid gap-4 sm:grid-cols-3"><Signal icon={BookOpen} label="Weekly study" value={minutes(data.weekly.reduce((sum, item) => sum + item.studyMinutes, 0))} /><Signal icon={Zap} label="Weekly XP" value={`${data.weekly.reduce((sum, item) => sum + item.xp, 0)}`} /><Signal icon={Flame} label="Habit check-ins" value={`${data.weekly.reduce((sum, item) => sum + item.habitCompletions, 0)}`} /></div><div className="mt-4 grid gap-4 xl:grid-cols-2"><ChartPanel title="Weekly study time" icon={BookOpen}><ResponsiveContainer width="100%" height={250}><BarChart data={data.weekly}><CartesianGrid stroke="#ffffff0b" vertical={false} /><XAxis dataKey="label" tick={{ fill: "#64748b", fontSize: 10 }} axisLine={false} tickLine={false} /><YAxis tick={{ fill: "#64748b", fontSize: 10 }} axisLine={false} tickLine={false} /><Tooltip {...tooltipStyle} /><Bar dataKey="studyMinutes" fill="#22d3ee" radius={[5, 5, 0, 0]} /></BarChart></ResponsiveContainer></ChartPanel><ChartPanel title="XP growth" icon={Zap}><ResponsiveContainer width="100%" height={250}><LineChart data={data.weekly}><CartesianGrid stroke="#ffffff0b" vertical={false} /><XAxis dataKey="label" tick={{ fill: "#64748b", fontSize: 10 }} axisLine={false} tickLine={false} /><YAxis tick={{ fill: "#64748b", fontSize: 10 }} axisLine={false} tickLine={false} /><Tooltip {...tooltipStyle} /><Line type="monotone" dataKey="xp" stroke="#fbbf24" strokeWidth={3} dot={{ fill: "#fbbf24", r: 3 }} /></LineChart></ResponsiveContainer></ChartPanel><ChartPanel title="Habit completion" icon={Flame}><ResponsiveContainer width="100%" height={250}><BarChart data={data.weekly}><CartesianGrid stroke="#ffffff0b" vertical={false} /><XAxis dataKey="label" tick={{ fill: "#64748b", fontSize: 10 }} axisLine={false} tickLine={false} /><YAxis allowDecimals={false} tick={{ fill: "#64748b", fontSize: 10 }} axisLine={false} tickLine={false} /><Tooltip {...tooltipStyle} /><Bar dataKey="habitCompletions" fill="#34d399" radius={[5, 5, 0, 0]} /></BarChart></ResponsiveContainer></ChartPanel><ChartPanel title="Study heatmap" icon={Sparkles}><div className="grid grid-cols-7 gap-2">{data.heatmap.map((item) => <div key={item.date} className="group relative aspect-square rounded-md" style={{ background: `rgba(34,211,238,${0.08 + item.minutes / maxHeat * 0.82})` }} title={`${item.date}: ${Math.round(item.minutes)} minutes`}><span className="sr-only">{item.date}: {Math.round(item.minutes)} minutes</span></div>)}</div><div className="mt-3 flex justify-between text-[10px] text-slate-600"><span>28 days ago</span><span>Today</span></div></ChartPanel><ChartPanel title="Subject progress" icon={BookOpen}><ResponsiveContainer width="100%" height={Math.max(220, data.subjects.length * 42)}><BarChart data={data.subjects} layout="vertical" margin={{ left: 10, right: 18 }}><XAxis type="number" domain={[0, 100]} hide /><YAxis type="category" dataKey="name" width={90} tick={{ fill: "#94a3b8", fontSize: 11 }} axisLine={false} tickLine={false} /><Tooltip {...tooltipStyle} /><Bar dataKey="progress" fill="#818cf8" radius={[0, 5, 5, 0]} /></BarChart></ResponsiveContainer></ChartPanel><ChartPanel title="Topic progress" icon={Route}><ResponsiveContainer width="100%" height={Math.max(220, data.topics.length * 34)}><BarChart data={data.topics} layout="vertical" margin={{ left: 10, right: 18 }}><XAxis type="number" domain={[0, 100]} hide /><YAxis type="category" dataKey="name" width={110} tick={{ fill: "#94a3b8", fontSize: 10 }} axisLine={false} tickLine={false} /><Tooltip {...tooltipStyle} /><Bar dataKey="progress" fill="#a78bfa" radius={[0, 5, 5, 0]} /></BarChart></ResponsiveContainer></ChartPanel></div><div className="mt-4 grid gap-4 lg:grid-cols-3"><ListPanel title="Achievements" icon={Award}>{data.achievements.map((item) => <div className={`state-row ${item.unlocked ? "text-amber-200" : "text-slate-600"}`} key={item.name}><Medal size={15} /><span>{item.name}</span>{item.unlocked && <Trophy size={14} className="ml-auto" />}</div>)}</ListPanel><ListPanel title="Personal records" icon={Trophy}>{data.records.map((item) => <div className="metric-tile" key={item.label}><strong>{item.value}</strong><span>{item.label}</span></div>)}</ListPanel><ListPanel title="Learning roadmap" icon={Map}>{data.roadmap.length ? data.roadmap.map((item) => <div className="state-row" key={item.title}><Route size={14} className="text-cyan-300" /><span className="min-w-0 flex-1 truncate">{item.title}</span><span className="text-xs text-slate-500">{Math.round(item.progress)}%</span></div>) : <Empty text="No roadmap items yet." />}</ListPanel></div><section className="glass-panel mt-4 p-5"><div className="mb-4 flex items-center gap-2"><Route size={17} className="text-cyan-300" /><h2 className="text-base font-medium text-white">Life journey</h2></div>{data.journey.length ? <div className="grid gap-2 md:grid-cols-2">{data.journey.map((item) => <div className="state-row" key={`${item.title}-${item.date}`}><div className="min-w-0 flex-1"><p className="truncate text-sm text-slate-200">{item.title}</p><p className="mt-1 text-xs text-slate-500">{item.detail}</p></div><time className="text-[10px] text-slate-600">{new Date(item.date).toLocaleDateString("en-US", { timeZone: "UTC" })}</time></div>)}</div> : <Empty text="Your journey will appear as milestones are completed." />}</section></main>;
}
function Signal({ icon: Icon, label, value }: { icon: typeof BookOpen; label: string; value: string }) { return <div className="stat-card border-cyan-400/20 bg-cyan-400/4"><div className="flex justify-between"><span className="eyebrow">{label}</span><Icon size={17} className="text-cyan-300" /></div><strong className="mt-4 block text-2xl font-medium text-white">{value}</strong></div>; }
function ChartPanel({ title, icon: Icon, children }: { title: string; icon: typeof BookOpen; children: ReactNode }) { return <section className="glass-panel min-w-0 p-5"><div className="mb-4 flex items-center gap-2"><Icon size={17} className="text-cyan-300" /><h2 className="text-sm font-medium text-white">{title}</h2></div>{children}</section>; }
function ListPanel({ title, icon: Icon, children }: { title: string; icon: typeof BookOpen; children: ReactNode }) { return <section className="glass-panel min-w-0 p-5"><div className="mb-4 flex items-center gap-2"><Icon size={17} className="text-cyan-300" /><h2 className="text-sm font-medium text-white">{title}</h2></div><div className="space-y-2">{children}</div></section>; }
function Empty({ text }: { text: string }) { return <div className="state-row justify-center text-xs">{text}</div>; }
